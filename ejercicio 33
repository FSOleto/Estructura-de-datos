#include <iostream>
using namespace std;

int main() {
    int n, a[101], x, p; cin >> n; for (int i = 0; i < n; ++i) cin >> a[i]; cin >> x >> p; for (int i = n; i > p; --i) a[i] = a[i - 1]; a[p] = x; ++n;
    for (int i = 0; i < n; ++i) cout << a[i] << (i + 1 == n ? '\n' : ' ');
    return 0;
}
