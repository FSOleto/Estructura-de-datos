#include <iostream>
using namespace std;

int main() {
    int n, a[100], p; cin >> n; for (int i = 0; i < n; ++i) cin >> a[i]; cin >> p; for (int i = p; i < n - 1; ++i) a[i] = a[i + 1]; --n;
    for (int i = 0; i < n; ++i) cout << a[i] << (i + 1 == n ? '\n' : ' ');
    return 0;
}
