#include <iostream>
using namespace std;

int main() {
    int n, a[100], x, m = 0; cin >> n; for (int i = 0; i < n; ++i) cin >> a[i]; cin >> x; for (int i = 0; i < n; ++i) if (a[i] != x) a[m++] = a[i];
    for (int i = 0; i < m; ++i) cout << a[i] << (i + 1 == m ? '\n' : ' ');
    return 0;
}
