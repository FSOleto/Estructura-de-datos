#include <iostream>
using namespace std;

int main() {
    int n, a[100], b[100]; long long suma = 0; cin >> n; for (int i = 0; i < n; ++i) cin >> a[i]; for (int i = 0; i < n; ++i) cin >> b[i];
    for (int i = 0; i < n; ++i) suma += a[i] * b[i]; cout << suma << '\n';
    return 0;
}
